@page "/"
@page "/search/{SearchText}/{Page:int}"
@page "/category/{CategoryUrl}"

<PageTitle>@pageTitle</PageTitle>

@if (SearchText is not { Length: > 0 } && CategoryUrl is not { Length: > 0 })
{
    <FeaturedProducts />
}
else
{
    <ProductList />
}
